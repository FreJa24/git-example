## hello commit

 ### What are the benefits of commits?
- You have a clear Project History.
- People can work at the same thing at the same time.
 ### Is there anther way to verify a commit was created?
- git show
 ### How to check if a fifle is tracked and if not, then track it? 
- git ls 
- if not: git add filename

### In which situation should you use `git diff`?
- if i want to see all changes to the last commit


### How do you create a patch with `git diff`?
- Create: Run git diff > my.patch
- This command saves your code changes into a file